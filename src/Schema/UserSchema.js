"use strict";

const debug = require( "debug" )( "Giggle-Node : UserSchema.js" );
const { Model } = require( "objection" );

class UserSchema extends Model
{
    constructor()
    {

    }
}

module.exports = UserSchema;